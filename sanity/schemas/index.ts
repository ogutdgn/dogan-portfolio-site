import { project } from './project';
import { blog } from './blog';
import { codeBlock } from './codeBlock';
import { tool } from './tool';

export const schemaTypes = [project, blog, codeBlock, tool];
